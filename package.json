{
  "name": "lorefell-forge",
  "private": true,
  "version": "0.1.0",
  "description": "LoreFell forge tooling: shared kernel served from Pages, CMS managed from CI.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "backup": "node scripts/backup.js",
    "create": "node scripts/createCollection.js",
    "seed": "node scripts/upsertItems.js",
    "apply": "node scripts/backup.js && node scripts/createCollection.js && node scripts/upsertItems.js",
    "canon": "node scripts/canonFromVault.js",
    "sync": "node scripts/canonFromVault.js && npm run apply",
    "contracts": "node scripts/checkContracts.js"
  }
}
